<div class="container">
  <div class="row">
    <!--FORM-->
    <div class="col-md-5 offset-md-3">
      <div class="card">
        <div class="card-body bg-dark">
          <h1 class="text-white">Editar Tarea</h1>

          <form action="/tasks/{{task._id}}/edit" method="post">
            <div class="mb-3">
              <input
                class="form-control"
                type="text"
                name="title"
                placeholder="Titulo"
                value="{{task.title}}"
              />
            </div>
            <div class="mb-3">
              <textarea
                class="form-control"
                name="description"
                rows="3"
                placeholder="Descripción"
              >{{task.description}}</textarea>
            </div>
            <button class="btn btn-primary w-100" type="submit">Actualizar</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>